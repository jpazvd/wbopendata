*! _lendingtype < : >                 by João Pedro Azevedo
*                 auto generated and updated using _update_countrymetadata.ado 
  
  program define _lendingtype 
  
  
         gen lendingtype = ""  
         replace lendingtype = "LNX"  if countrycode == "ABW"  
         replace lendingtype = "IDX"  if countrycode == "AFG"  
         replace lendingtype = "IBD"  if countrycode == "AGO"  
         replace lendingtype = "IBD"  if countrycode == "ALB"  
         replace lendingtype = "LNX"  if countrycode == "AND"  
         replace lendingtype = "LNX"  if countrycode == "ARE"  
         replace lendingtype = "IBD"  if countrycode == "ARG"  
         replace lendingtype = "IBD"  if countrycode == "ARM"  
         replace lendingtype = "LNX"  if countrycode == "ASM"  
         replace lendingtype = "IBD"  if countrycode == "ATG"  
         replace lendingtype = "LNX"  if countrycode == "AUS"  
         replace lendingtype = "LNX"  if countrycode == "AUT"  
         replace lendingtype = "IBD"  if countrycode == "AZE"  
         replace lendingtype = "IDX"  if countrycode == "BDI"  
         replace lendingtype = "LNX"  if countrycode == "BEL"  
         replace lendingtype = "IDX"  if countrycode == "BEN"  
         replace lendingtype = "IDX"  if countrycode == "BFA"  
         replace lendingtype = "IDX"  if countrycode == "BGD"  
         replace lendingtype = "IBD"  if countrycode == "BGR"  
         replace lendingtype = "LNX"  if countrycode == "BHR"  
         replace lendingtype = "LNX"  if countrycode == "BHS"  
         replace lendingtype = "IBD"  if countrycode == "BIH"  
         replace lendingtype = "IBD"  if countrycode == "BLR"  
         replace lendingtype = "IBD"  if countrycode == "BLZ"  
         replace lendingtype = "LNX"  if countrycode == "BMU"  
         replace lendingtype = "IBD"  if countrycode == "BOL"  
         replace lendingtype = "IBD"  if countrycode == "BRA"  
         replace lendingtype = "LNX"  if countrycode == "BRB"  
         replace lendingtype = "LNX"  if countrycode == "BRN"  
         replace lendingtype = "IDX"  if countrycode == "BTN"  
         replace lendingtype = "IBD"  if countrycode == "BWA"  
         replace lendingtype = "IDX"  if countrycode == "CAF"  
         replace lendingtype = "LNX"  if countrycode == "CAN"  
         replace lendingtype = "LNX"  if countrycode == "CHE"  
         replace lendingtype = "LNX"  if countrycode == "CHI"  
         replace lendingtype = "IBD"  if countrycode == "CHL"  
         replace lendingtype = "IBD"  if countrycode == "CHN"  
         replace lendingtype = "IDX"  if countrycode == "CIV"  
         replace lendingtype = "IDB"  if countrycode == "CMR"  
         replace lendingtype = "IDX"  if countrycode == "COD"  
         replace lendingtype = "IDB"  if countrycode == "COG"  
         replace lendingtype = "IBD"  if countrycode == "COL"  
         replace lendingtype = "IDX"  if countrycode == "COM"  
         replace lendingtype = "IDB"  if countrycode == "CPV"  
         replace lendingtype = "IBD"  if countrycode == "CRI"  
         replace lendingtype = "LNX"  if countrycode == "CUB"  
         replace lendingtype = "LNX"  if countrycode == "CUW"  
         replace lendingtype = "LNX"  if countrycode == "CYM"  
         replace lendingtype = "LNX"  if countrycode == "CYP"  
         replace lendingtype = "LNX"  if countrycode == "CZE"  
         replace lendingtype = "LNX"  if countrycode == "DEU"  
         replace lendingtype = "IDX"  if countrycode == "DJI"  
         replace lendingtype = "IDB"  if countrycode == "DMA"  
         replace lendingtype = "LNX"  if countrycode == "DNK"  
         replace lendingtype = "IBD"  if countrycode == "DOM"  
         replace lendingtype = "IBD"  if countrycode == "DZA"  
         replace lendingtype = "IBD"  if countrycode == "ECU"  
         replace lendingtype = "IBD"  if countrycode == "EGY"  
         replace lendingtype = "IDX"  if countrycode == "ERI"  
         replace lendingtype = "LNX"  if countrycode == "ESP"  
         replace lendingtype = "LNX"  if countrycode == "EST"  
         replace lendingtype = "IDX"  if countrycode == "ETH"  
         replace lendingtype = "LNX"  if countrycode == "FIN"  
         replace lendingtype = "IBD"  if countrycode == "FJI"  
         replace lendingtype = "LNX"  if countrycode == "FRA"  
         replace lendingtype = "LNX"  if countrycode == "FRO"  
         replace lendingtype = "IDX"  if countrycode == "FSM"  
         replace lendingtype = "IBD"  if countrycode == "GAB"  
         replace lendingtype = "LNX"  if countrycode == "GBR"  
         replace lendingtype = "IBD"  if countrycode == "GEO"  
         replace lendingtype = "IDX"  if countrycode == "GHA"  
         replace lendingtype = "LNX"  if countrycode == "GIB"  
         replace lendingtype = "IDX"  if countrycode == "GIN"  
         replace lendingtype = "IDX"  if countrycode == "GMB"  
         replace lendingtype = "IDX"  if countrycode == "GNB"  
         replace lendingtype = "IBD"  if countrycode == "GNQ"  
         replace lendingtype = "LNX"  if countrycode == "GRC"  
         replace lendingtype = "IDB"  if countrycode == "GRD"  
         replace lendingtype = "LNX"  if countrycode == "GRL"  
         replace lendingtype = "IBD"  if countrycode == "GTM"  
         replace lendingtype = "LNX"  if countrycode == "GUM"  
         replace lendingtype = "IDX"  if countrycode == "GUY"  
         replace lendingtype = "LNX"  if countrycode == "HKG"  
         replace lendingtype = "IDX"  if countrycode == "HND"  
         replace lendingtype = "IBD"  if countrycode == "HRV"  
         replace lendingtype = "IDX"  if countrycode == "HTI"  
         replace lendingtype = "LNX"  if countrycode == "HUN"  
         replace lendingtype = "IBD"  if countrycode == "IDN"  
         replace lendingtype = "LNX"  if countrycode == "IMN"  
         replace lendingtype = "IBD"  if countrycode == "IND"  
         replace lendingtype = "LNX"  if countrycode == "IRL"  
         replace lendingtype = "IBD"  if countrycode == "IRN"  
         replace lendingtype = "IBD"  if countrycode == "IRQ"  
         replace lendingtype = "LNX"  if countrycode == "ISL"  
         replace lendingtype = "LNX"  if countrycode == "ISR"  
         replace lendingtype = "LNX"  if countrycode == "ITA"  
         replace lendingtype = "IBD"  if countrycode == "JAM"  
         replace lendingtype = "IBD"  if countrycode == "JOR"  
         replace lendingtype = "LNX"  if countrycode == "JPN"  
         replace lendingtype = "IBD"  if countrycode == "KAZ"  
         replace lendingtype = "IDB"  if countrycode == "KEN"  
         replace lendingtype = "IDX"  if countrycode == "KGZ"  
         replace lendingtype = "IDX"  if countrycode == "KHM"  
         replace lendingtype = "IDX"  if countrycode == "KIR"  
         replace lendingtype = "IBD"  if countrycode == "KNA"  
         replace lendingtype = "LNX"  if countrycode == "KOR"  
         replace lendingtype = "LNX"  if countrycode == "KWT"  
         replace lendingtype = "IDX"  if countrycode == "LAO"  
         replace lendingtype = "IBD"  if countrycode == "LBN"  
         replace lendingtype = "IDX"  if countrycode == "LBR"  
         replace lendingtype = "IBD"  if countrycode == "LBY"  
         replace lendingtype = "IDB"  if countrycode == "LCA"  
         replace lendingtype = "LNX"  if countrycode == "LIE"  
         replace lendingtype = "IBD"  if countrycode == "LKA"  
         replace lendingtype = "IDX"  if countrycode == "LSO"  
         replace lendingtype = "LNX"  if countrycode == "LTU"  
         replace lendingtype = "LNX"  if countrycode == "LUX"  
         replace lendingtype = "LNX"  if countrycode == "LVA"  
         replace lendingtype = "LNX"  if countrycode == "MAC"  
         replace lendingtype = "LNX"  if countrycode == "MAF"  
         replace lendingtype = "IBD"  if countrycode == "MAR"  
         replace lendingtype = "LNX"  if countrycode == "MCO"  
         replace lendingtype = "IDB"  if countrycode == "MDA"  
         replace lendingtype = "IDX"  if countrycode == "MDG"  
         replace lendingtype = "IDX"  if countrycode == "MDV"  
         replace lendingtype = "IBD"  if countrycode == "MEX"  
         replace lendingtype = "IDX"  if countrycode == "MHL"  
         replace lendingtype = "IBD"  if countrycode == "MKD"  
         replace lendingtype = "IDX"  if countrycode == "MLI"  
         replace lendingtype = "LNX"  if countrycode == "MLT"  
         replace lendingtype = "IDX"  if countrycode == "MMR"  
         replace lendingtype = "IBD"  if countrycode == "MNE"  
         replace lendingtype = "IDB"  if countrycode == "MNG"  
         replace lendingtype = "LNX"  if countrycode == "MNP"  
         replace lendingtype = "IDX"  if countrycode == "MOZ"  
         replace lendingtype = "IDX"  if countrycode == "MRT"  
         replace lendingtype = "IBD"  if countrycode == "MUS"  
         replace lendingtype = "IDX"  if countrycode == "MWI"  
         replace lendingtype = "IBD"  if countrycode == "MYS"  
         replace lendingtype = "IBD"  if countrycode == "NAM"  
         replace lendingtype = "LNX"  if countrycode == "NCL"  
         replace lendingtype = "IDX"  if countrycode == "NER"  
         replace lendingtype = "IDB"  if countrycode == "NGA"  
         replace lendingtype = "IDX"  if countrycode == "NIC"  
         replace lendingtype = "LNX"  if countrycode == "NLD"  
         replace lendingtype = "LNX"  if countrycode == "NOR"  
         replace lendingtype = "IDX"  if countrycode == "NPL"  
         replace lendingtype = "IBD"  if countrycode == "NRU"  
         replace lendingtype = "LNX"  if countrycode == "NZL"  
         replace lendingtype = "LNX"  if countrycode == "OMN"  
         replace lendingtype = "IDB"  if countrycode == "PAK"  
         replace lendingtype = "IBD"  if countrycode == "PAN"  
         replace lendingtype = "IBD"  if countrycode == "PER"  
         replace lendingtype = "IBD"  if countrycode == "PHL"  
         replace lendingtype = "IBD"  if countrycode == "PLW"  
         replace lendingtype = "IDB"  if countrycode == "PNG"  
         replace lendingtype = "IBD"  if countrycode == "POL"  
         replace lendingtype = "LNX"  if countrycode == "PRI"  
         replace lendingtype = "LNX"  if countrycode == "PRK"  
         replace lendingtype = "LNX"  if countrycode == "PRT"  
         replace lendingtype = "IBD"  if countrycode == "PRY"  
         replace lendingtype = "LNX"  if countrycode == "PSE"  
         replace lendingtype = "LNX"  if countrycode == "PYF"  
         replace lendingtype = "LNX"  if countrycode == "QAT"  
         replace lendingtype = "IBD"  if countrycode == "ROU"  
         replace lendingtype = "IBD"  if countrycode == "RUS"  
         replace lendingtype = "IDX"  if countrycode == "RWA"  
         replace lendingtype = "LNX"  if countrycode == "SAU"  
         replace lendingtype = "IDX"  if countrycode == "SDN"  
         replace lendingtype = "IDX"  if countrycode == "SEN"  
         replace lendingtype = "LNX"  if countrycode == "SGP"  
         replace lendingtype = "IDX"  if countrycode == "SLB"  
         replace lendingtype = "IDX"  if countrycode == "SLE"  
         replace lendingtype = "IBD"  if countrycode == "SLV"  
         replace lendingtype = "LNX"  if countrycode == "SMR"  
         replace lendingtype = "IDX"  if countrycode == "SOM"  
         replace lendingtype = "IBD"  if countrycode == "SRB"  
         replace lendingtype = "IDX"  if countrycode == "SSD"  
         replace lendingtype = "IDX"  if countrycode == "STP"  
         replace lendingtype = "IBD"  if countrycode == "SUR"  
         replace lendingtype = "LNX"  if countrycode == "SVK"  
         replace lendingtype = "LNX"  if countrycode == "SVN"  
         replace lendingtype = "LNX"  if countrycode == "SWE"  
         replace lendingtype = "IBD"  if countrycode == "SWZ"  
         replace lendingtype = "LNX"  if countrycode == "SXM"  
         replace lendingtype = "IBD"  if countrycode == "SYC"  
         replace lendingtype = "IDX"  if countrycode == "SYR"  
         replace lendingtype = "LNX"  if countrycode == "TCA"  
         replace lendingtype = "IDX"  if countrycode == "TCD"  
         replace lendingtype = "IDX"  if countrycode == "TGO"  
         replace lendingtype = "IBD"  if countrycode == "THA"  
         replace lendingtype = "IDX"  if countrycode == "TJK"  
         replace lendingtype = "IBD"  if countrycode == "TKM"  
         replace lendingtype = "IDB"  if countrycode == "TLS"  
         replace lendingtype = "IDX"  if countrycode == "TON"  
         replace lendingtype = "IBD"  if countrycode == "TTO"  
         replace lendingtype = "IBD"  if countrycode == "TUN"  
         replace lendingtype = "IBD"  if countrycode == "TUR"  
         replace lendingtype = "IDX"  if countrycode == "TUV"  
         replace lendingtype = "LNX"  if countrycode == "TWN"  
         replace lendingtype = "IDX"  if countrycode == "TZA"  
         replace lendingtype = "IDX"  if countrycode == "UGA"  
         replace lendingtype = "IBD"  if countrycode == "UKR"  
         replace lendingtype = "IBD"  if countrycode == "URY"  
         replace lendingtype = "LNX"  if countrycode == "USA"  
         replace lendingtype = "IDB"  if countrycode == "UZB"  
         replace lendingtype = "IDB"  if countrycode == "VCT"  
         replace lendingtype = "IBD"  if countrycode == "VEN"  
         replace lendingtype = "LNX"  if countrycode == "VGB"  
         replace lendingtype = "LNX"  if countrycode == "VIR"  
         replace lendingtype = "IBD"  if countrycode == "VNM"  
         replace lendingtype = "IDX"  if countrycode == "VUT"  
         replace lendingtype = "IDX"  if countrycode == "WSM"  
         replace lendingtype = "IDX"  if countrycode == "XKX"  
         replace lendingtype = "IDX"  if countrycode == "YEM"  
  
  
  end 
